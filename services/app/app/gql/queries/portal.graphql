query CompanyUpdatePortal($input: ContentHashQueryInput!, $leadersAlias: String) {
  contentHash(input: $input) {
    id
    name
    hash
    labels
    primarySite {
      id
    }
    isLeader: hasWebsiteSchedule(input: { sectionAlias: $leadersAlias })
  }
}
