<div class="col-12">
  <h6 class="text-muted">{{ category.name }}</h6>
</div>
{{#each sections as |section|}}
  {{directory-sections-review/section
    section=section
    selected=selected
    sectionIds=sectionIds
    canSelect=canSelect
    onToggle=onToggle
  }}
{{/each}}
