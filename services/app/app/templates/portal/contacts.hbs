<div class="card mb-4">
  <div class="card-body">
    <p class="lead">{{ i18n config.locale "contactsP1Label" }}</p>
    <p>{{ i18n config.locale "companyDetailsP2Label" }}</p>
    <p>{{ i18n config.locale "companyDetailsP3Label" }}</p>
  </div>
</div>

<form {{action "showModal" on="submit"}}>

  <div class="row">
    {{#each contacts as |contact|}}
      {{contact-card
        contact=contact
        onRemove=(action "remove" contact)
        onUpdate=(action "update" contact)
        onReset=(action "reset" contact)
        locale=config.locale
      }}
    {{/each}}
  </div>

  <button type="button" class="btn btn-primary" {{action "add" }}>
    {{entypo-icon "circle-with-plus"}} {{ i18n config.locale "addContactLabel"}}
  </button>

  <div class="fixed-bottom fixed-right card">
    <div class="card-body">
      <button disabled={{isSubmitDisabled}} class="btn btn-success pull-right" type="submit">
        {{entypo-icon "save"}} {{ i18n config.locale "saveChangesLabel"}}
      </button>
    </div>
  </div>
</form>

{{submit-modal/contacts
  isOpen=isModalOpen
  hash=hash
  payload=payload
  onComplete=(action "transitionToPortal")
}}
