<div class="card mb-4">
  <div class="card-body">
    <p class="lead">{{ i18n config.lang "contactsP1Label" }}</p>
    <p>{{ i18n config.lang "companyDetailsP2Label" }}</p>
    <p>{{ i18n config.lang "companyDetailsP3Label" }}</p>
  </div>
</div>

<form {{action "showModal" on="submit"}}>

  <div class="row">
    {{#each contacts as |contact|}}
      {{contact-card
        contact=contact
        onRemove=(action "remove" contact)
        onUpdate=(action "update" contact)
        onReset=(action "reset" contact)
        lang=config.lang
      }}
    {{/each}}
  </div>

  <button type="button" class="btn btn-primary" {{action "add" }}>
    {{entypo-icon "circle-with-plus"}} {{ i18n config.lang "addContactLabel"}}
  </button>

  <div class="fixed-bottom fixed-right card">
    <div class="card-body">
      <button disabled={{isSubmitDisabled}} class="btn btn-success pull-right" type="submit">
        {{entypo-icon "save"}} {{ i18n config.lang "saveChangesLabel"}}
      </button>
    </div>
  </div>
</form>

{{submit-modal/contacts
  isOpen=isModalOpen
  hash=hash
  payload=payload
  onComplete=(action "transitionToPortal")
}}
